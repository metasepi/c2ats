sudo: required
dist: trusty

before_install:
  - date
  - gcc --version
  - sudo apt-get -qq -y install haskell-platform cabal-install
  - cabal update
  - export PATH=${HOME}/.cabal/bin:${PATH}

script:
  - cabal install
  - (cd example/complex && make test)

after_script:
  - date
